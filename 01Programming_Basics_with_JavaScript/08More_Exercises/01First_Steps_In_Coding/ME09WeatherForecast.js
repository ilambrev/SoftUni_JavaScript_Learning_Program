function weatherForecast(weather) {
    console.log(weather == "sunny" ? "It's warm outside!" : "It's cold outside!");
}

// weatherForecast('sunny');
// weatherForecast('cloudy');
// weatherForecast('snowy');