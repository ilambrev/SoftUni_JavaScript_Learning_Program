const Loading = () => {
    return (

        // Loading spinner
        <div className="loading-shade">
            < div className="spinner" ></div >
        </div>

    );
};

export default Loading;